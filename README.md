# colorprofilesourcemanager
A KWin script that automatically disable &amp; restore icc profile when certain windows are active and fullscreen.

Note that this script assumes the dbus service `nl.dvdgiessen.dbusapplauncher` exists, which is used to run `kscreen-doctor`.